openapi: 3.0.3
info:
  title: 健身房後端 API — 最終作業規格書
  version: 1.0.0
  description: |
    ## 這本規格書怎麼用

    左側依**里程碑**分組（M1 → M6 就是建議的施工順序）。每支 API 都有
    request / response 範例：先看範例、再看欄位說明，需要登入的 API
    按右上角 **Authorize** 貼上 login 拿到的 token，就能直接 **Try it out** 試打你的後端。

    ## 全域回應格式（每一支都適用）

    | 情況 | HTTP 狀態碼 | Body |
    |---|---|---|
    | 成功 | 2xx | `{ "status": "success", "data": ... }` |
    | 可預期的失敗（驗證錯、找不到、沒權限） | 4xx | `{ "status": "failed", "message": "..." }` |
    | 你的程式壞掉 | 5xx | （不在驗收範圍，但請努力不要發生） |

    - **驗收只看 2xx / 4xx + status 欄位**：文件裡寫的 200/201 是參考值，回 200 或 201 都算對
    - 需要登入的 API 一律收 `Authorization: Bearer <token>`
    - JWT payload 必須含 `{ id, role, exp }`（前端會解開來用，缺了會壞）
    - 所有 id 都是 uuid 字串
    - 日期時間欄位用 UTC ISO 8601 字串（例 `2026-08-20T10:00:00Z`）

    ## ⚠️ 四句固定錯誤訊息文字（全文件最重要的一段）

    以下四句錯誤訊息，前端會拿來判斷要顯示哪個畫面，必須一字不差：

    1. `已經報名過此課程`
    2. `已無可使用堂數`
    3. `已達最大參加人數，無法參加`
    4. `請先登入`

    其他錯誤訊息文字隨你寫（只要 status 是 failed）。

servers:
  - url: http://localhost:8080
    description: 你的後端（本機開發）

tags:
  - name: M0 系統
    description: 健康檢查 — CI 等你就緒、Docker 看你活著，全靠這支。先做。
  - name: "M1 種資料：技能與方案"
    description: 「做完這組 → 前端的健身方案頁活了。驗證：npm run test:m1」
  - name: M2 會員系統
    description: 「做完這組 → 註冊、登入、會員中心活了。驗證：npm run test:m2」
  - name: M3 教練後台
    description: 「做完這組 → 教練個人後台、課程管理活了。驗證：npm run test:m3」
  - name: M4 公開瀏覽
    description: 「做完這組 → 教練列表、教練詳情活了。驗證：npm run test:m4」
  - name: M5 購買與報名
    description: 「重要：四句固定錯誤訊息文字都在這。做完 → 購買方案、報名課程、我的課表活了。驗證：npm run test:m5」
  - name: M6 月營收（挑戰）
    description: 「⭐ 挑戰級：聚合統計。做完 → 教練營收報表活了。驗證：npm run test:m6」
  - name: 加分題
    description: 選做，不列入驗收。

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: 用 POST /api/users/login 拿到的 token

paths:
  # ===== g1-skill-package =====
  /healthcheck:
    get:
      tags:
        - "M0 系統"
      summary: 服務健康檢查，回純文字 OK
      description: |-
        確認後端服務活著用的端點，不需登入、不需任何參數。
        ⚠️ 三個地雷一次講清楚：
        1. 回應是**純文字 `OK`**，不是 JSON。這是整份文件唯一不套 `{ status, data }` 包裝的端點，請不要改成 JSON。
        2. 路徑是 `/healthcheck`，**不在 `/api` 底下**。
        3. 公約④＋第 10 週 docker-compose 都靠它：CI 和 backend 容器的 healthcheck 會打這支，確認你的 server 已啟動且資料庫可用。沒做這支，GitHub Actions 會卡在等待後端就緒，容器化驗收也會失敗。
      responses:
        "200":
          description: 服務正常運作（這支沒有可預期的失敗回應；打不通就代表服務沒起來）
          content:
            text/plain:
              schema:
                type: string
                example: OK
  /api/coaches/skill:
    get:
      tags:
        - "M1 種資料：技能與方案"
      summary: 取得教練技能列表
      description: |-
        回傳全部技能，不需登入。前端有三個地方共用這支：管理者的技能管理頁、教練個人資料頁、開課表單的技能下拉選單。
        ⚠️ 路由順序地雷：這條路徑必須排在 `/api/coaches/{coachId}`（教練詳情）**之前**。兩條路徑長得都像「/api/coaches/後面接一段文字」，如果教練詳情那條先註冊，`skill` 這個字會被當成 coachId 吃掉——這支 API 就永遠打不到。掛路由時請把這條放前面。
      responses:
        "200":
          description: 成功取得技能列表（還沒有任何技能時回空陣列 `[]`，不是錯誤）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: 技能 id（uuid 字串）
                        name:
                          type: string
                          description: 技能名稱
              example:
                status: success
                data:
                  - id: "2a4b6c8d-1e2f-4a5b-9c8d-0e1f2a3b4c5d"
                    name: "重訓"
                  - id: "5f6e7d8c-9b0a-4c3d-8e7f-6a5b4c3d2e1f"
                    name: "瑜伽"
    post:
      tags:
        - "M1 種資料：技能與方案"
      summary: 新增教練技能
      description: |-
        新增一筆技能，不需登入（課程簡化設計：這是管理用的「種資料」端點，正式產品會加權限控管）。
        技能名稱不可重複。
        ⚠️ 成功狀態碼是 **200，不是 201**（答案版實際行為）。驗收只看「成功 2xx ＋ status 欄位」，所以你回 201 也算過，但文件以 200 為準。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  description: 技能名稱，必填、不可為空字串或全空白
            example:
              name: "重訓"
      responses:
        "200":
          description: 新增成功，回傳完整的技能資料（含後端產生的 id 與建立時間）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        description: 後端產生的 uuid
                      name:
                        type: string
                      createdAt:
                        type: string
                        description: 建立時間（ISO 8601 字串）
              example:
                status: success
                data:
                  id: "2a4b6c8d-1e2f-4a5b-9c8d-0e1f2a3b4c5d"
                  name: "重訓"
                  createdAt: "2026-06-10T08:00:00.000Z"
        "400":
          description: 觸發條件：沒給 name、name 不是字串、或 name 是空字串／全空白
          content:
            application/json:
              example:
                status: failed
                message: "欄位未填寫正確"
        "409":
          description: 觸發條件：name 與既有技能重複
          content:
            application/json:
              example:
                status: failed
                message: "資料重複"
  /api/coaches/skill/{skillId}:
    delete:
      tags:
        - "M1 種資料：技能與方案"
      summary: 刪除教練技能
      description: |-
        依 id 刪除一筆技能，不需登入（同樣是課程簡化的管理端點）。
        成功回應的 `data` 內容不列入驗收——答案版回一個刪除結果物件（見範例），你回 `null` 也可以，驗收只看 2xx ＋ `status: "success"`。
        ⚠️ skillId 必須是合法的 uuid 字串：格式正確但查無資料 → 400「ID錯誤」；**根本不是 uuid 格式**（例如隨便打 `abc`）不列入驗收，建議你回 400 failed，不要讓它變成 500。
      parameters:
        - name: skillId
          in: path
          required: true
          description: 要刪除的技能 id（uuid 字串）
          schema:
            type: string
          example: "2a4b6c8d-1e2f-4a5b-9c8d-0e1f2a3b4c5d"
      responses:
        "200":
          description: 刪除成功（data 內容不列入驗收）
          content:
            application/json:
              example:
                status: success
                data:
                  raw: []
                  affected: 1
        "400":
          description: 觸發條件：skillId 是合法 uuid 格式，但查無這筆技能
          content:
            application/json:
              example:
                status: failed
                message: "ID錯誤"
  /api/credit-package:
    get:
      tags:
        - "M1 種資料：技能與方案"
      summary: 取得購買方案列表
      description: |-
        回傳全部堂數方案，不需登入。前端的「健身方案」頁直接吃這份資料。
        ⚠️ `price` 的型別：答案版回的是**數字字串**（例如 `"1400.00"`），你回**數字**（例如 `1400`）也可以——前端兩種都處理得了，不構成驗收差異。文件範例照答案版寫字串。
      responses:
        "200":
          description: 成功取得方案列表（還沒有任何方案時回空陣列 `[]`，不是錯誤）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: 方案 id（uuid 字串）
                        name:
                          type: string
                          description: 方案名稱
                        credit_amount:
                          type: integer
                          description: 此方案包含的堂數
                        price:
                          description: 價格。數字或數字字串皆可（答案版回字串 "1400.00"）
                          oneOf:
                            - type: number
                            - type: string
              example:
                status: success
                data:
                  - id: "0d2c1b3a-4e5f-4a6b-8c7d-9e0f1a2b3c4d"
                    name: "7 堂組合包方案"
                    credit_amount: 7
                    price: "1400.00"
                  - id: "1e3d2c4b-5f6a-4b7c-9d8e-0f1a2b3c4d5e"
                    name: "14 堂組合包方案"
                    credit_amount: 14
                    price: "2520.00"
    post:
      tags:
        - "M1 種資料：技能與方案"
      summary: 新增購買方案
      description: |-
        新增一筆堂數方案，不需登入（課程簡化的管理端點；前端頁面沒有呼叫它，但它是把「健身方案」資料種進系統的唯一入口——不先用這支種資料，方案頁會永遠空白、購買流程也無從驗收）。
        方案名稱不可重複。`credit_amount` 與 `price` 都必須是 **0 以上的整數（數字型別）**——price 請送數字，不要送字串，送字串會被擋 400。
        ⚠️ 成功狀態碼是 **200，不是 201**（答案版實際行為）。驗收只看「成功 2xx ＋ status 欄位」，回 201 也算過。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - credit_amount
                - price
              properties:
                name:
                  type: string
                  description: 方案名稱，必填、不可為空字串或全空白、不可與既有方案重複
                credit_amount:
                  type: integer
                  description: 方案包含的堂數，必填、0 以上的整數
                price:
                  type: integer
                  description: 方案價格，必填、0 以上的整數（要送數字型別，不能送字串）
            example:
              name: "7 堂組合包方案"
              credit_amount: 7
              price: 1400
      responses:
        "200":
          description: 新增成功，回傳完整的方案資料（含後端產生的 id 與建立時間）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        description: 後端產生的 uuid
                      name:
                        type: string
                      credit_amount:
                        type: integer
                      price:
                        type: number
                      createdAt:
                        type: string
                        description: 建立時間（ISO 8601 字串）
              example:
                status: success
                data:
                  id: "0d2c1b3a-4e5f-4a6b-8c7d-9e0f1a2b3c4d"
                  name: "7 堂組合包方案"
                  credit_amount: 7
                  price: 1400
                  createdAt: "2026-06-10T08:00:00.000Z"
        "400":
          description: 觸發條件：任一欄位沒給；name 不是字串或為空；credit_amount 或 price 不是數字、是負數、或帶小數
          content:
            application/json:
              example:
                status: failed
                message: "欄位未填寫正確"
        "409":
          description: 觸發條件：name 與既有方案重複
          content:
            application/json:
              example:
                status: failed
                message: "資料重複"
  /api/credit-package/{creditPackageId}:
    post:
      tags:
        - "M5 購買與報名"
      summary: 購買堂數方案（需登入）
      description: |-
        登入的使用者購買指定方案，替自己加值堂數。
        **body 完全留空**——購買的堂數與金額由後端依 creditPackageId 從方案資料帶入，前端不送也不能送（設計理由：金額如果由前端送，使用者改一下封包就能 1 元買 100 堂）。
        購買後沒有「剩餘堂數」欄位可以直接查：剩餘堂數＝歷次購買堂數加總 − 未取消的報名數，由後端即時計算（見 M5 報名相關端點）。
        ⚠️ 固定錯誤訊息文字：未帶 token 打這支時，錯誤訊息必須**一字不差**回「請先登入」——前端靠這句文字決定要不要把使用者導去登入頁，多一個字、少一個標點都會讓前端壞掉。
        驗收只看「成功 2xx／失敗 4xx ＋ status 欄位」＋上述固定錯誤訊息文字，狀態碼數字為參考值。
      security:
        - bearerAuth: []
      parameters:
        - name: creditPackageId
          in: path
          required: true
          description: 要購買的方案 id（uuid 字串）
          schema:
            type: string
          example: "0d2c1b3a-4e5f-4a6b-8c7d-9e0f1a2b3c4d"
      responses:
        "200":
          description: 購買成功，`data` 固定回 `null`（不回購買明細）
          content:
            application/json:
              example:
                status: success
                data: null
        "400":
          description: 觸發條件：creditPackageId 查無對應方案
          content:
            application/json:
              example:
                status: failed
                message: "ID錯誤"
        "401":
          description: |
            觸發條件：登入驗證沒過。下方是建議回應範例；驗收重點是 4xx + status: failed。
            只有「請先登入」是固定錯誤訊息文字，必須完全相同。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                missing:
                  summary: 沒帶 Authorization header，或 header 裡沒有 token
                  value:
                    status: failed
                    message: "請先登入"
                expired:
                  summary: token 已過期
                  value:
                    status: failed
                    message: "Token 已過期"
                invalid:
                  summary: token 無效，或 token 裡的使用者已不存在
                  value:
                    status: failed
                    message: "無效的 token"
    delete:
      tags:
        - "M1 種資料：技能與方案"
      summary: 刪除購買方案
      description: |-
        依 id 刪除一筆方案，不需登入（課程簡化的管理端點，前端頁面沒有呼叫它）。
        成功回應的 `data` 內容不列入驗收——答案版回一個刪除結果物件（見範例），你回 `null` 也可以，驗收只看 2xx ＋ `status: "success"`。
        ⚠️ creditPackageId 必須是合法的 uuid 字串：格式正確但查無資料 → 400「ID錯誤」；**不是 uuid 格式**不列入驗收，建議你回 400 failed，不要讓它變成 500。
      parameters:
        - name: creditPackageId
          in: path
          required: true
          description: 要刪除的方案 id（uuid 字串）
          schema:
            type: string
          example: "0d2c1b3a-4e5f-4a6b-8c7d-9e0f1a2b3c4d"
      responses:
        "200":
          description: 刪除成功（data 內容不列入驗收）
          content:
            application/json:
              example:
                status: success
                data:
                  raw: []
                  affected: 1
        "400":
          description: 觸發條件：creditPackageId 是合法 uuid 格式，但查無這筆方案
          content:
            application/json:
              example:
                status: failed
                message: "ID錯誤"
  # ===== g2-users =====
  /api/users/signup:
    post:
      tags:
        - M2 會員系統
      summary: 註冊新會員帳號
      description: |
        建立一個新會員。身分（role）固定是 USER，註冊時無法指定身分。

        行為備註：
        - 密碼規則由後端把關：必須同時包含英文大寫、英文小寫、數字，長度 8～16 字。不符合規則回 400，錯誤訊息可參考下方範例。
        - Email 不可重複，已被註冊過回 409。
        - body 多帶的欄位（例如前端表單順手送來的 confirmPassword）會被忽略；「兩次密碼是否一致」是前端的事，這支 API 不檢查。
        - 成功只回 user 的 id（uuid 字串）與 name，不回 email、絕對不回密碼。
        - ⚠️ 成功狀態碼是 201（不是 200）。驗收尺度：成功＝2xx ＋ body.status 為 "success"、可預期失敗＝4xx ＋ body.status 為 "failed"，文中的數字是實際值供參考。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - email
                - password
              properties:
                name:
                  type: string
                  description: 使用者暱稱，不可為空字串或空白
                  example: 小華
                email:
                  type: string
                  description: 登入用 Email，全站不可重複
                  example: xiaohua@example.com
                password:
                  type: string
                  description: 必須同時含英文大小寫與數字，8～16 字
                  example: Hexschool12345
            example:
              name: 小華
              email: xiaohua@example.com
              password: Hexschool12345
      responses:
        "201":
          description: 註冊成功（⚠️ 是 201 不是 200）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      user:
                        type: object
                        properties:
                          id:
                            type: string
                            description: uuid 字串
                            example: 0fd96b3c-6e2a-4f2b-8d3e-1c2a4b5d6e7f
                          name:
                            type: string
                            example: 小華
              example:
                status: success
                data:
                  user:
                    id: 0fd96b3c-6e2a-4f2b-8d3e-1c2a4b5d6e7f
                    name: 小華
        "400":
          description: |
            欄位驗證失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。
            - name、email、password 任一缺漏或為空字串：「欄位未填寫正確」
            - 密碼不符合規則（缺大寫／小寫／數字、或長度不在 8～16）：「密碼不符合規則，需要包含英文數字大小寫，最短8個字，最長16個字」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                欄位缺漏:
                  summary: 任一必填欄位缺漏或為空
                  value:
                    status: failed
                    message: 欄位未填寫正確
                密碼不符規則:
                  summary: 密碼不符合大小寫英數 8~16 字規則
                  value:
                    status: failed
                    message: 密碼不符合規則，需要包含英文數字大小寫，最短8個字，最長16個字
        "409":
          description: Email 已經被別人註冊過
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              example:
                status: failed
                message: Email 已被使用
  /api/users/login:
    post:
      tags:
        - M2 會員系統
      summary: 會員登入，取得 JWT token
      description: |
        用 Email 與密碼登入，成功會拿到一組 token，之後所有需要登入的 API 都把它放在
        Authorization header（格式：Bearer <token>）一起送出。

        行為備註：
        - ⚠️ JWT payload 格式：解開 token 後必須包含 { id, role, exp } 三個欄位
          （id 是使用者 uuid、role 是 "USER" 或 "COACH"、exp 是過期時間）。前端會自己解析 token
          讀這三個欄位來決定顯示哪些選單，少任何一個前端就會壞掉。
        - ⚠️ 「帳號不存在」與「密碼錯誤」共用同一句 400 訊息「使用者不存在或密碼輸入錯誤」——
          刻意不告訴別人這個 Email 有沒有註冊過，兩種情況都要回一模一樣的訊息。
        - 登入時也會先檢查欄位與密碼規則：密碼格式根本不符合規則時直接回 400（連帳號都不會去查）。
        - 成功回 data.token 與 data.user.name（只有 name，沒有其他欄位）。
        - ⚠️ 成功狀態碼是 201（不是 200）。驗收尺度：只看 2xx／4xx ＋ status 欄位，數字供參考。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                  description: 註冊時使用的 Email
                  example: xiaohua@example.com
                password:
                  type: string
                  description: 登入密碼
                  example: Hexschool12345
            example:
              email: xiaohua@example.com
              password: Hexschool12345
      responses:
        "201":
          description: 登入成功（⚠️ 是 201 不是 200）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                        description: JWT token，payload 必含 { id, role, exp }
                        example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjBmZDk2YjNjIiwicm9sZSI6IlVTRVIiLCJleHAiOjE3NTAwMDAwMDB9.signature
                      user:
                        type: object
                        properties:
                          name:
                            type: string
                            example: 小華
              example:
                status: success
                data:
                  token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjBmZDk2YjNjIiwicm9sZSI6IlVTRVIiLCJleHAiOjE3NTAwMDAwMDB9.signature
                  user:
                    name: 小華
        "400":
          description: |
            登入失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。
            - email 或 password 缺漏／空字串：「欄位未填寫正確」
            - 密碼格式不符合規則（此時不會去查帳號）：「密碼不符合規則，需要包含英文數字大小寫，最短8個字，最長16個字」
            - 帳號不存在「或」密碼比對錯誤（⚠️ 兩種情況共用同一句）：「使用者不存在或密碼輸入錯誤」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                欄位缺漏:
                  summary: email 或 password 缺漏／空字串
                  value:
                    status: failed
                    message: 欄位未填寫正確
                密碼不符規則:
                  summary: 密碼格式不符合規則
                  value:
                    status: failed
                    message: 密碼不符合規則，需要包含英文數字大小寫，最短8個字，最長16個字
                帳密錯誤:
                  summary: 帳號不存在或密碼錯誤（共用訊息）
                  value:
                    status: failed
                    message: 使用者不存在或密碼輸入錯誤
  /api/users/profile:
    get:
      tags:
        - M2 會員系統
      summary: 取得本人的個人資料
      description: |
        回傳目前登入者的 name 與 email（只有這兩個欄位）。

        行為備註：
        - 前端每次切換頁面（只要身上有 token）都會打一次這支來確認登入狀態——
          這支沒做好，整個登入後的畫面都進不去。
        - 成功 200，data.user 形狀固定是 { name, email }。
      security:
        - bearerAuth: []
      responses:
        "200":
          description: 成功取得個人資料
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      user:
                        type: object
                        properties:
                          name:
                            type: string
                            example: 小華
                          email:
                            type: string
                            example: xiaohua@example.com
              example:
                status: success
                data:
                  user:
                    name: 小華
                    email: xiaohua@example.com
        "401":
          description: |
            token 驗證失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。只有「請先登入」是固定錯誤訊息文字，必須完全相同。
            - 沒帶 Authorization header、或格式不是 Bearer <token>：「請先登入」
            - token 已過期：「Token 已過期」
            - token 無效（內容不對、或查無此使用者）：「無效的 token」
            ⚠️「請先登入」是四句固定錯誤訊息文字之一，一個字都不能改。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header 或缺 token
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無此使用者
                  value:
                    status: failed
                    message: 無效的 token
    put:
      tags:
        - M2 會員系統
      summary: 更新本人的暱稱
      description: |
        更新目前登入者的暱稱（name），一次只能改這一個欄位。

        行為備註：
        - ⚠️ 新名稱跟目前的名稱「一模一樣」會直接失敗，回 400「使用者名稱未變更」——
          這不是 bug，是規格。前端沒有擋「沒改就送出」，使用者沒改名就按儲存，
          看到這個錯誤訊息是預期行為，請不要把它改成成功。
        - 成功 200，回更新後的 data.user（只有 name 欄位）。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                  description: 新的暱稱，不可為空字串，且必須與目前名稱不同
                  example: 小華二號
            example:
              name: 小華二號
      responses:
        "200":
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      user:
                        type: object
                        properties:
                          name:
                            type: string
                            example: 小華二號
              example:
                status: success
                data:
                  user:
                    name: 小華二號
        "400":
          description: |
            更新失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。
            - name 缺漏或為空字串：「欄位未填寫正確」
            - ⚠️ 新名稱與目前名稱相同：「使用者名稱未變更」
            - 更新沒有生效（極少見的邊角情況）：「更新使用者資料失敗」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                欄位缺漏:
                  summary: name 缺漏或為空字串
                  value:
                    status: failed
                    message: 欄位未填寫正確
                名稱未變更:
                  summary: 新名稱與目前名稱相同
                  value:
                    status: failed
                    message: 使用者名稱未變更
                更新失敗:
                  summary: 更新沒有生效（邊角情況）
                  value:
                    status: failed
                    message: 更新使用者資料失敗
        "401":
          description: |
            token 驗證失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。只有「請先登入」是固定錯誤訊息文字，必須完全相同。
            - 沒帶 Authorization header、或格式不是 Bearer <token>：「請先登入」
            - token 已過期：「Token 已過期」
            - token 無效（內容不對、或查無此使用者）：「無效的 token」
            ⚠️「請先登入」是四句固定錯誤訊息文字之一，一個字都不能改。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header 或缺 token
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無此使用者
                  value:
                    status: failed
                    message: 無效的 token
  /api/users/password:
    put:
      tags:
        - M2 會員系統
      summary: 修改本人的登入密碼
      description: |
        用舊密碼換新密碼。body 固定三個欄位：password（舊密碼）、new_password（新密碼）、
        confirm_new_password（再輸入一次新密碼）。

        行為備註（檢查依下列順序進行，踩到哪條就回哪條的訊息）：
        1. 三個欄位任一缺漏或為空字串 → 400「欄位未填寫正確」
        2. ⚠️ 三個欄位「全部」都要通過密碼規則（含英文大小寫與數字、8～16 字）——
           不是只檢查新密碼，舊密碼格式不符也會擋 → 400「密碼不符合規則，需要包含英文數字大小寫，最短8個字，最長16個字」
        3. ⚠️ 新密碼跟舊密碼相同會被擋 → 400「新密碼不能與舊密碼相同」
        4. new_password 與 confirm_new_password 不一致 → 400「新密碼與驗證新密碼不一致」
        5. 舊密碼比對錯誤 → 400「密碼輸入錯誤」
        - 成功 200，data 是 null（沒有任何資料要回）。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - password
                - new_password
                - confirm_new_password
              properties:
                password:
                  type: string
                  description: 目前的舊密碼（也要符合密碼規則）
                  example: Hexschool12345
                new_password:
                  type: string
                  description: 新密碼，須符合密碼規則且不可與舊密碼相同
                  example: NewHexschool99
                confirm_new_password:
                  type: string
                  description: 再輸入一次新密碼，必須與 new_password 完全一致
                  example: NewHexschool99
            example:
              password: Hexschool12345
              new_password: NewHexschool99
              confirm_new_password: NewHexschool99
      responses:
        "200":
          description: 修改成功，data 為 null
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    nullable: true
                    example: null
              example:
                status: success
                data: null
        "400":
          description: |
            修改失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。
            - 三欄任一缺漏／空字串：「欄位未填寫正確」
            - 三欄任一不符密碼規則：「密碼不符合規則，需要包含英文數字大小寫，最短8個字，最長16個字」
            - 新密碼與舊密碼相同：「新密碼不能與舊密碼相同」
            - 新密碼與確認新密碼不一致：「新密碼與驗證新密碼不一致」
            - 舊密碼比對錯誤：「密碼輸入錯誤」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                欄位缺漏:
                  summary: 三欄任一缺漏或為空字串
                  value:
                    status: failed
                    message: 欄位未填寫正確
                密碼不符規則:
                  summary: 三欄任一不符合密碼規則
                  value:
                    status: failed
                    message: 密碼不符合規則，需要包含英文數字大小寫，最短8個字，最長16個字
                新舊相同:
                  summary: 新密碼與舊密碼相同
                  value:
                    status: failed
                    message: 新密碼不能與舊密碼相同
                確認不一致:
                  summary: 新密碼與確認新密碼不一致
                  value:
                    status: failed
                    message: 新密碼與驗證新密碼不一致
                舊密碼錯誤:
                  summary: 舊密碼比對錯誤
                  value:
                    status: failed
                    message: 密碼輸入錯誤
        "401":
          description: |
            token 驗證失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。只有「請先登入」是固定錯誤訊息文字，必須完全相同。
            - 沒帶 Authorization header、或格式不是 Bearer <token>：「請先登入」
            - token 已過期：「Token 已過期」
            - token 無效（內容不對、或查無此使用者）：「無效的 token」
            ⚠️「請先登入」是四句固定錯誤訊息文字之一，一個字都不能改。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header 或缺 token
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無此使用者
                  value:
                    status: failed
                    message: 無效的 token
  /api/users/credit-package:
    get:
      tags:
        - M5 購買與報名
      summary: 取得本人的購買方案紀錄
      description: |
        回傳目前登入者買過的所有堂數方案紀錄。data 直接是「陣列」（不是物件包陣列），
        沒買過任何方案時回空陣列 []。

        行為備註：
        - 每筆形狀固定：{ name（方案名稱）, purchased_credits（買到的堂數）,
          price_paid（付了多少錢，數字）, purchase_at（購買時間）}。
        - ⚠️ price_paid 回「數字」型別（例如 1400，不是字串 "1400.00"）。
        - 排序：依 purchase_at 新到舊（最新買的排最前面）。
      security:
        - bearerAuth: []
      responses:
        "200":
          description: 成功取得購買紀錄（沒買過回空陣列）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                          description: 方案名稱
                          example: 14 堂組合包方案
                        purchased_credits:
                          type: integer
                          description: 這筆購買取得的堂數
                          example: 14
                        price_paid:
                          type: integer
                          description: 實際支付金額（數字型別）
                          example: 1400
                        purchase_at:
                          type: string
                          description: 購買時間（UTC ISO 8601）
                          example: 2026-05-10T03:20:00.000Z
              example:
                status: success
                data:
                  - name: 14 堂組合包方案
                    purchased_credits: 14
                    price_paid: 1400
                    purchase_at: "2026-05-10T03:20:00.000Z"
                  - name: 7 堂組合包方案
                    purchased_credits: 7
                    price_paid: 1400
                    purchase_at: "2026-04-01T09:00:00.000Z"
        "401":
          description: |
            token 驗證失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。只有「請先登入」是固定錯誤訊息文字，必須完全相同。
            - 沒帶 Authorization header、或格式不是 Bearer <token>：「請先登入」
            - token 已過期：「Token 已過期」
            - token 無效（內容不對、或查無此使用者）：「無效的 token」
            ⚠️「請先登入」是四句固定錯誤訊息文字之一，一個字都不能改。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header 或缺 token
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無此使用者
                  value:
                    status: failed
                    message: 無效的 token
  /api/users/courses:
    get:
      tags:
        - M5 購買與報名
      summary: 取得本人的課表與剩餘堂數
      description: |
        回傳目前登入者的剩餘堂數、已使用堂數、以及所有報名過的課程清單（含已取消）。

        data 形狀固定三個欄位：
        - credit_remain：剩餘堂數
        - credit_usage：已使用堂數
        - course_booking：報名紀錄陣列

        行為備註：
        - ⚠️ credit_remain 的口徑：剩餘堂數 ＝「全部購買的堂數加總」−「未取消的報名數」。
          沒有任何地方存「餘額」這個欄位，每次都要用這條公式現算；
          已取消的報名不佔堂數（取消會把堂數還回來）。
        - credit_usage ＝ 未取消的報名數（跟上面公式的右半邊是同一個數字）。
        - ⚠️ course_booking「包含已取消的報名」——cancelled_at 有值代表已取消、null 代表有效。
          前端靠 cancelled_at 來顯示狀態，不要自作主張把已取消的過濾掉。
        - 排序：依課程 start_at 由舊到新（ASC）。
        - 每筆形狀：{ course_id, name, start_at, end_at, meeting_url, coach_name, cancelled_at }，
          coach_name 是開課教練的名稱、course_id 是 uuid 字串。
      security:
        - bearerAuth: []
      responses:
        "200":
          description: 成功取得課表（從未報名過時 course_booking 為空陣列）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      credit_remain:
                        type: integer
                        description: 剩餘堂數 ＝ Σ購買堂數 − 未取消報名數
                        example: 12
                      credit_usage:
                        type: integer
                        description: 已使用堂數（未取消的報名數）
                        example: 2
                      course_booking:
                        type: array
                        description: 報名紀錄（含已取消），依 start_at 由舊到新
                        items:
                          type: object
                          properties:
                            course_id:
                              type: string
                              description: 課程 id（uuid 字串）
                              example: 5b1c3e6a-2f4d-4e8b-9a7c-d1e2f3a4b5c6
                            name:
                              type: string
                              description: 課程名稱
                              example: 瑜伽入門體驗
                            start_at:
                              type: string
                              description: 上課開始時間（UTC ISO 8601）
                              example: 2026-08-20T10:00:00.000Z
                            end_at:
                              type: string
                              description: 上課結束時間（UTC ISO 8601）
                              example: 2026-08-20T12:00:00.000Z
                            meeting_url:
                              type: string
                              description: 線上教室連結
                              example: https://meet.example.com/yoga-101
                            coach_name:
                              type: string
                              description: 開課教練的名稱
                              example: 教練小美
                            cancelled_at:
                              type: string
                              nullable: true
                              description: 取消時間；null 代表此報名仍有效
                              example: null
              example:
                status: success
                data:
                  credit_remain: 12
                  credit_usage: 2
                  course_booking:
                    - course_id: 5b1c3e6a-2f4d-4e8b-9a7c-d1e2f3a4b5c6
                      name: 瑜伽入門體驗
                      start_at: "2026-08-20T10:00:00.000Z"
                      end_at: "2026-08-20T12:00:00.000Z"
                      meeting_url: https://meet.example.com/yoga-101
                      coach_name: 教練小美
                      cancelled_at: null
                    - course_id: 7a2d4f8b-3c5e-4a9d-b1f2-e3a4b5c6d7e8
                      name: 重訓基礎課
                      start_at: "2026-09-01T13:00:00.000Z"
                      end_at: "2026-09-01T15:00:00.000Z"
                      meeting_url: https://meet.example.com/fitness-201
                      coach_name: 教練小明
                      cancelled_at: "2026-08-15T06:30:00.000Z"
        "401":
          description: |
            token 驗證失敗。下方是建議回應範例；驗收重點是 4xx + status: failed。只有「請先登入」是固定錯誤訊息文字，必須完全相同。
            - 沒帶 Authorization header、或格式不是 Bearer <token>：「請先登入」
            - token 已過期：「Token 已過期」
            - token 無效（內容不對、或查無此使用者）：「無效的 token」
            ⚠️「請先登入」是四句固定錯誤訊息文字之一，一個字都不能改。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header 或缺 token
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無此使用者
                  value:
                    status: failed
                    message: 無效的 token
  # ===== g3-admin-coach =====
  /api/admin/coaches/{userId}:
    post:
      tags:
        - M3 教練後台
      summary: 將指定使用者升級為教練
      description: |
        把一般使用者（role 為 USER）升級成教練：建立一筆教練資料，並把該使用者的 role 改成 COACH。升級成功後，這位使用者就能登入使用教練後台的功能。

        ⚠️ **安全性註記（課程簡化）**：這支 API 是 public、**不需要登入**就能呼叫。真實世界的系統絕對不會讓任何人都能把別人升級成教練——正式產品這種端點一定要做身分與權限控管。本課程為了讓大家專注在主流程，刻意簡化，請記得這不是正常設計。

        行為備註：
        - `profile_image_url` 是**選填**：可以整個不給、也可以給空字串，都會過。但只要有給非空的值，就必須以 `https` 開頭，否則回 400「欄位未填寫正確」。
        - ⚠️ 注意：這條規則跟「更新教練資料（PUT /api/admin/coaches）」**不一樣**（那支是必填），對照表請看 PUT 那支的說明。
        - 同一個使用者只能升級一次：已經是教練再打一次會回 409「使用者已經是教練」。
        - 文件寫的狀態碼（201/400/409）是答案版實際行為，供參考；**驗收只看「成功 2xx / 失敗 4xx」加上回應的 status 欄位**，不逐碼比對。
      parameters:
        - name: userId
          in: path
          required: true
          description: 要升級的使用者 id（uuid 字串）
          schema:
            type: string
            format: uuid
          example: "f3b7d2c1-8a4e-4c6f-9d2b-5e1a7c3f9b8d"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - experience_years
                - description
              properties:
                experience_years:
                  type: integer
                  minimum: 0
                  description: 教練年資（數字、不能是負數或小數）
                description:
                  type: string
                  description: 教練介紹（不能是空字串）
                profile_image_url:
                  type: string
                  description: 大頭貼網址。選填，可不給或給空字串；但只要有值就必須以 https 開頭
              example:
                experience_years: 3
                description: "瑜伽與皮拉提斯雙修，擅長帶初學者"
                profile_image_url: "https://example.com/avatar.png"
      responses:
        "201":
          description: 升級成功，回傳更新後的使用者（role 已變 COACH）與新建立的教練資料
          content:
            application/json:
              example:
                status: success
                data:
                  user:
                    name: "小美"
                    role: "COACH"
                  coach:
                    id: "a1c5e8f2-3b7d-4e9a-8c6f-2d4b9e1a7c3f"
                    user_id: "f3b7d2c1-8a4e-4c6f-9d2b-5e1a7c3f9b8d"
                    experience_years: 3
                    description: "瑜伽與皮拉提斯雙修，擅長帶初學者"
                    profile_image_url: "https://example.com/avatar.png"
                    created_at: "2026-08-20T10:00:00.000Z"
                    updated_at: "2026-08-20T10:00:00.000Z"
        "400":
          description: |
            可預期的失敗，依情境回不同訊息：
            - 欄位缺漏或格式不對（experience_years 不是 0 以上的整數、description 是空字串、profile_image_url 有值但不是 https 開頭）→「欄位未填寫正確」
            - userId 查不到對應的使用者 →「使用者不存在」
          content:
            application/json:
              examples:
                欄位未填寫正確:
                  summary: 欄位缺漏、格式錯誤、或大頭貼網址不是 https 開頭
                  value:
                    status: failed
                    message: "欄位未填寫正確"
                使用者不存在:
                  summary: userId 查無此使用者
                  value:
                    status: failed
                    message: "使用者不存在"
        "409":
          description: 該使用者已經是教練（重複升級）
          content:
            application/json:
              example:
                status: failed
                message: "使用者已經是教練"
  /api/admin/coaches:
    get:
      tags:
        - M3 教練後台
      summary: 取得教練本人的後台資料（含技能清單）
      description: |
        教練登入後查看自己的教練資料，通常用來把資料塞進「編輯教練資料」的表單。需要登入，而且登入的人必須已經是教練。

        行為備註：
        - 回傳的 `id` 是**教練資料的 id**，不是使用者 id，兩個是不同的東西，不要拿去當 userId 用。
        - `skill_ids` 是這位教練目前綁定的技能 id 清單（uuid 字串陣列）；還沒設定過技能時會回空陣列 `[]`。
        - 剛升級、還沒填過大頭貼的教練，`profile_image_url` 可能是 null。⚠️ 把這份資料塞進表單後直接按儲存會吃 400，原因看 PUT 那支的說明。

        ⚠️ **固定錯誤訊息文字**：沒帶 token 時回的「請先登入」是前端會直接拿來判斷畫面的四句固定錯誤訊息文字之一，**一字不能差**（不能寫成「請登入」「尚未登入」等變體），否則前端成品會壞掉。

        文件寫的狀態碼是答案版實際行為，供參考；**驗收只看「成功 2xx / 失敗 4xx」加上回應的 status 欄位**。
      security:
        - bearerAuth: []
      responses:
        "200":
          description: 成功取得教練本人資料
          content:
            application/json:
              example:
                status: success
                data:
                  id: "a1c5e8f2-3b7d-4e9a-8c6f-2d4b9e1a7c3f"
                  experience_years: 3
                  description: "瑜伽與皮拉提斯雙修，擅長帶初學者"
                  profile_image_url: "https://example.com/avatar.png"
                  skill_ids:
                    - "7e2f9a4b-1c8d-4f6e-b3a5-9d7c2e4f8a1b"
                    - "3c6e1f8a-5b2d-4a9c-8e7f-1b4d6a9c3e5f"
        "401":
          description: |
            登入或身分檢查失敗，依情境回不同訊息：
            - 沒帶 Authorization header 或沒帶 token →「請先登入」（⚠️ 固定錯誤訊息文字，一字不差）
            - token 已過期 →「Token 已過期」
            - token 無效或查無此使用者 →「無效的 token」
            - 有登入但還不是教練 →「使用者尚未成為教練」（注意：這裡回的是 401，不是 403）
          content:
            application/json:
              examples:
                請先登入:
                  summary: 沒帶 token（⚠️ 訊息為固定錯誤訊息文字）
                  value:
                    status: failed
                    message: "請先登入"
                Token 已過期:
                  summary: token 過期
                  value:
                    status: failed
                    message: "Token 已過期"
                無效的 token:
                  summary: token 無效或查無使用者
                  value:
                    status: failed
                    message: "無效的 token"
                使用者尚未成為教練:
                  summary: 已登入但 role 不是 COACH
                  value:
                    status: failed
                    message: "使用者尚未成為教練"
    put:
      tags:
        - M3 教練後台
      summary: 更新教練本人的後台資料（含整批更換技能）
      description: |
        教練更新自己的年資、介紹、大頭貼與技能清單。需要登入，而且登入的人必須已經是教練。成功後回傳更新完的同形狀資料，前端可以直接拿來覆蓋表單。

        ⚠️ **profile_image_url 規則跟「升級教練」那支不一樣**，這是最容易踩的坑，對照表如下：

        | 規則 | 升級教練 POST /api/admin/coaches/{userId} | 更新教練資料 PUT /api/admin/coaches |
        |---|---|---|
        | 是否必填 | 選填（可不給、可空字串） | **必填**（不給或空字串直接 400） |
        | 格式要求 | 有值時必須以 https 開頭 | 必須以 https 開頭 |

        ⚠️ 連鎖地雷：升級時沒填大頭貼的教練，GET 拿到的 `profile_image_url` 是 null；前端把它原樣塞進表單、使用者沒補就按儲存，這支會回 400「欄位未填寫正確」。做前端串接時記得引導使用者先補上 https 開頭的網址。

        行為備註：
        - `skill_ids` 是**整批覆蓋**：你送出的清單會完全取代原本的技能清單（不是「新增」也不是「合併」）。想保留原本的技能，就要連舊的一起送。
        - `skill_ids` 必須是**非空陣列**，且每個元素都是技能的 id（uuid 字串）；空陣列回 400「欄位未填寫正確」。
        - 文件寫的狀態碼是答案版實際行為，供參考；**驗收只看「成功 2xx / 失敗 4xx」加上回應的 status 欄位**。
        - 沒帶 token 回的「請先登入」是固定錯誤訊息文字（同 GET 的說明），一字不能差。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - experience_years
                - description
                - profile_image_url
                - skill_ids
              properties:
                experience_years:
                  type: integer
                  minimum: 0
                  description: 教練年資（數字、不能是負數或小數）
                description:
                  type: string
                  description: 教練介紹（不能是空字串）
                profile_image_url:
                  type: string
                  description: 大頭貼網址。這支是必填，且必須以 https 開頭（⚠️ 跟升級教練那支的選填規則不同）
                skill_ids:
                  type: array
                  minItems: 1
                  items:
                    type: string
                    format: uuid
                  description: 技能 id 清單（整批覆蓋原有技能，不能是空陣列）
              example:
                experience_years: 5
                description: "重訓與體態雕塑，協助學員建立規律訓練習慣"
                profile_image_url: "https://example.com/new-avatar.png"
                skill_ids:
                  - "7e2f9a4b-1c8d-4f6e-b3a5-9d7c2e4f8a1b"
                  - "3c6e1f8a-5b2d-4a9c-8e7f-1b4d6a9c3e5f"
      responses:
        "200":
          description: 更新成功，回傳更新後的教練資料（形狀與 GET 相同，可直接覆蓋表單）
          content:
            application/json:
              example:
                status: success
                data:
                  id: "a1c5e8f2-3b7d-4e9a-8c6f-2d4b9e1a7c3f"
                  experience_years: 5
                  description: "重訓與體態雕塑，協助學員建立規律訓練習慣"
                  profile_image_url: "https://example.com/new-avatar.png"
                  skill_ids:
                    - "7e2f9a4b-1c8d-4f6e-b3a5-9d7c2e4f8a1b"
                    - "3c6e1f8a-5b2d-4a9c-8e7f-1b4d6a9c3e5f"
        "400":
          description: |
            欄位缺漏或格式不對，一律回「欄位未填寫正確」。會觸發的情境：
            - experience_years 不是 0 以上的整數
            - description 沒給或是空字串
            - profile_image_url 沒給、是空字串、或不是 https 開頭（⚠️ 這支是必填）
            - skill_ids 沒給、不是陣列、是空陣列、或元素不是有效的 id 字串
          content:
            application/json:
              example:
                status: failed
                message: "欄位未填寫正確"
        "401":
          description: |
            登入或身分檢查失敗，依情境回不同訊息：
            - 沒帶 Authorization header 或沒帶 token →「請先登入」（⚠️ 固定錯誤訊息文字，一字不差）
            - token 已過期 →「Token 已過期」
            - token 無效或查無此使用者 →「無效的 token」
            - 有登入但還不是教練 →「使用者尚未成為教練」（注意：這裡回的是 401，不是 403）
          content:
            application/json:
              examples:
                請先登入:
                  summary: 沒帶 token（⚠️ 訊息為固定錯誤訊息文字）
                  value:
                    status: failed
                    message: "請先登入"
                Token 已過期:
                  summary: token 過期
                  value:
                    status: failed
                    message: "Token 已過期"
                無效的 token:
                  summary: token 無效或查無使用者
                  value:
                    status: failed
                    message: "無效的 token"
                使用者尚未成為教練:
                  summary: 已登入但 role 不是 COACH
                  value:
                    status: failed
                    message: "使用者尚未成為教練"
  # ===== g4-admin-courses =====
  /api/admin/coaches/courses:
    get:
      tags:
        - M3 教練後台
      summary: 取得教練本人開設的全部課程列表
      description: |
        教練後台「課程管理」頁的列表資料。需要登入，且登入者必須已經是教練（role 為 COACH）。

        行為備註：
        - 只會回「自己開的課」，依登入者的 token 判斷，不會看到別的教練的課程。
        - ⚠️ `status` 是三態中文字串（`尚未開始`／`進行中`／`已結束`），由伺服器**收到請求的當下時間**即時計算，不是存起來的欄位。同一堂課今天查是「尚未開始」，開課後再查就變「進行中」。
        - ⚠️ `participants` 是該課程「未取消」的報名數——已取消的報名不計。
        - 還沒開過任何課時回 `data: []`（空陣列），這是成功回應，不是錯誤。
        - 驗收口徑：文件標的狀態碼數字供參考，驗收只看 2xx／4xx 與 body 的 `status` 欄位。
      security:
        - bearerAuth: []
      responses:
        "200":
          description: 成功取得課程列表（沒開過課時 data 為空陣列）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: array
                    description: 教練本人的課程陣列，無課程時為空陣列
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: 課程 id（uuid）
                          example: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
                        name:
                          type: string
                          example: 重訓基礎入門
                        status:
                          type: string
                          description: 三態中文字串，用當下時間即時計算
                          enum:
                            - 尚未開始
                            - 進行中
                            - 已結束
                          example: 尚未開始
                        start_at:
                          type: string
                          description: 課程開始時間（UTC ISO 8601）
                          example: "2026-08-20T10:00:00.000Z"
                        end_at:
                          type: string
                          description: 課程結束時間（UTC ISO 8601）
                          example: "2026-08-20T12:00:00.000Z"
                        max_participants:
                          type: integer
                          example: 10
                        meeting_url:
                          type: string
                          example: "https://meet.example.com/abc-defg-hij"
                        participants:
                          type: integer
                          description: 未取消的報名數。⚠️ 答案版有報名時會回數字字串（如 "2"）、無報名時回數字 0，驗收兩種型別都收，建議統一回數字
                          example: 2
              example:
                status: success
                data:
                  - id: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
                    name: 重訓基礎入門
                    status: 尚未開始
                    start_at: "2026-08-20T10:00:00.000Z"
                    end_at: "2026-08-20T12:00:00.000Z"
                    max_participants: 10
                    meeting_url: "https://meet.example.com/abc-defg-hij"
                    participants: 2
        "401":
          description: |
            未登入、token 有問題、或登入者不是教練時觸發。
            ⚠️ 「請先登入」是前端會拿來判斷畫面的固定錯誤訊息文字，一字不差。
            觸發條件對照：沒帶 Authorization header 或格式不對 →「請先登入」；token 過期 →「Token 已過期」；token 無效或查無使用者 →「無效的 token」；登入了但不是教練 →「使用者尚未成為教練」（注意是 401 不是 403）。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無使用者
                  value:
                    status: failed
                    message: 無效的 token
                不是教練:
                  summary: 登入者 role 不是 COACH
                  value:
                    status: failed
                    message: 使用者尚未成為教練
    post:
      tags:
        - M3 教練後台
      summary: 教練開設新課程
      description: |
        教練後台「新增課程」modal 送出時呼叫。需要登入，且登入者必須已經是教練。

        行為備註：
        - ⚠️ 全部欄位都是必填，缺一個、或是空字串，就回「欄位未填寫正確」。
        - ⚠️ `meeting_url` 必須以 `https` 開頭，否則一樣回「欄位未填寫正確」。
        - ⚠️ `max_participants` 必須是「數字型別」的 0 以上整數——送字串 `"10"` 會被擋下來。
        - `start_at`／`end_at` 收 UTC ISO 8601 字串（例 `2026-08-20T10:00:00Z`），原樣存入、原樣吐回。
        - 開課的教練就是 token 裡的本人，body 不用（也不能）指定教練。
        - 成功回 201（不是 200），data.course 是完整課程物件。驗收口徑：狀態碼數字供參考，驗收只看 2xx／4xx 與 body 的 `status` 欄位。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - skill_id
                - name
                - description
                - start_at
                - end_at
                - max_participants
                - meeting_url
              properties:
                skill_id:
                  type: string
                  description: 技能 id（uuid），來自 GET /api/coaches/skill 的下拉選單
                  example: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
                name:
                  type: string
                  example: 重訓基礎入門
                description:
                  type: string
                  example: 從零開始的重訓課，教你正確使用器材與姿勢。
                start_at:
                  type: string
                  description: 課程開始時間，UTC ISO 8601 字串
                  example: "2026-08-20T10:00:00Z"
                end_at:
                  type: string
                  description: 課程結束時間，UTC ISO 8601 字串
                  example: "2026-08-20T12:00:00Z"
                max_participants:
                  type: integer
                  description: ⚠️ 必須是數字型別的 0 以上整數，字串會被擋
                  example: 10
                meeting_url:
                  type: string
                  description: ⚠️ 必須以 https 開頭
                  example: "https://meet.example.com/abc-defg-hij"
            example:
              skill_id: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
              name: 重訓基礎入門
              description: 從零開始的重訓課，教你正確使用器材與姿勢。
              start_at: "2026-08-20T10:00:00Z"
              end_at: "2026-08-20T12:00:00Z"
              max_participants: 10
              meeting_url: "https://meet.example.com/abc-defg-hij"
      responses:
        "201":
          description: 開課成功，回完整課程物件（包在 data.course 裡）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      course:
                        type: object
                        description: 完整課程物件
                        properties:
                          id:
                            type: string
                            example: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
                          user_id:
                            type: string
                            description: 開課教練的使用者 id（uuid）
                            example: 9a8b7c6d-5e4f-3a2b-1c0d-e1f2a3b4c5d6
                          skill_id:
                            type: string
                            example: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
                          name:
                            type: string
                            example: 重訓基礎入門
                          description:
                            type: string
                            example: 從零開始的重訓課，教你正確使用器材與姿勢。
                          start_at:
                            type: string
                            example: "2026-08-20T10:00:00.000Z"
                          end_at:
                            type: string
                            example: "2026-08-20T12:00:00.000Z"
                          max_participants:
                            type: integer
                            example: 10
                          meeting_url:
                            type: string
                            example: "https://meet.example.com/abc-defg-hij"
                          created_at:
                            type: string
                            example: "2026-08-01T03:00:00.000Z"
                          updated_at:
                            type: string
                            example: "2026-08-01T03:00:00.000Z"
              example:
                status: success
                data:
                  course:
                    id: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
                    user_id: 9a8b7c6d-5e4f-3a2b-1c0d-e1f2a3b4c5d6
                    skill_id: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
                    name: 重訓基礎入門
                    description: 從零開始的重訓課，教你正確使用器材與姿勢。
                    start_at: "2026-08-20T10:00:00.000Z"
                    end_at: "2026-08-20T12:00:00.000Z"
                    max_participants: 10
                    meeting_url: "https://meet.example.com/abc-defg-hij"
                    created_at: "2026-08-01T03:00:00.000Z"
                    updated_at: "2026-08-01T03:00:00.000Z"
        "400":
          description: |
            欄位驗證失敗時觸發：任一欄位缺漏或為空字串、max_participants 不是數字型別的 0 以上整數、meeting_url 不是 https 開頭。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              example:
                status: failed
                message: 欄位未填寫正確
        "401":
          description: |
            未登入、token 有問題、或登入者不是教練時觸發。
            ⚠️ 「請先登入」是前端會拿來判斷畫面的固定錯誤訊息文字，一字不差。
            觸發條件對照：沒帶 token →「請先登入」；token 過期 →「Token 已過期」；token 無效 →「無效的 token」；不是教練 →「使用者尚未成為教練」。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無使用者
                  value:
                    status: failed
                    message: 無效的 token
                不是教練:
                  summary: 登入者 role 不是 COACH
                  value:
                    status: failed
                    message: 使用者尚未成為教練
  /api/admin/coaches/courses/{courseId}:
    get:
      tags:
        - M3 教練後台
      summary: 取得單一課程詳情（編輯課程時的初始值）
      description: |
        教練後台「編輯課程」modal 打開時呼叫，把課程現況塞回表單。需要登入。

        行為備註：
        - ⚠️ 這支是 owner-scoped：用「課程 id ＋ token 裡的本人 id」一起查。課程不是你開的、或 id 根本不存在，**都回同一句 400「課程不存在」**——不會告訴你「這堂課存在但不是你的」，避免洩漏別的教練的課程。
        - ⚠️ 回傳是「扁平物件」——data 直接就是課程欄位，**沒有**再包一層 `course`（跟開課／更新課程的回傳形狀不同，前端表單就吃這個形狀）。
        - 扁平物件裡多了 `skill_name` 與 `skill_id` 兩欄，給表單的技能下拉選單用。
        - 這支只驗登入、不驗教練身分：一般會員帶 token 來查，會因為查不到「自己開的課」拿到 400「課程不存在」。
        - 驗收口徑：狀態碼數字供參考，驗收只看 2xx／4xx 與 body 的 `status` 欄位。
      security:
        - bearerAuth: []
      parameters:
        - name: courseId
          in: path
          required: true
          description: 課程 id（uuid）
          schema:
            type: string
          example: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
      responses:
        "200":
          description: 成功取得課程詳情（data 是扁平物件，不包 course）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    description: ⚠️ 扁平物件，欄位直接攤在 data 底下
                    properties:
                      id:
                        type: string
                        example: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
                      name:
                        type: string
                        example: 重訓基礎入門
                      description:
                        type: string
                        example: 從零開始的重訓課，教你正確使用器材與姿勢。
                      start_at:
                        type: string
                        example: "2026-08-20T10:00:00.000Z"
                      end_at:
                        type: string
                        example: "2026-08-20T12:00:00.000Z"
                      max_participants:
                        type: integer
                        example: 10
                      skill_name:
                        type: string
                        description: 技能名稱（給表單顯示用）
                        example: 重量訓練
                      skill_id:
                        type: string
                        description: 技能 id（給下拉選單選中用）
                        example: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
                      meeting_url:
                        type: string
                        example: "https://meet.example.com/abc-defg-hij"
              example:
                status: success
                data:
                  id: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
                  name: 重訓基礎入門
                  description: 從零開始的重訓課，教你正確使用器材與姿勢。
                  start_at: "2026-08-20T10:00:00.000Z"
                  end_at: "2026-08-20T12:00:00.000Z"
                  max_participants: 10
                  skill_name: 重量訓練
                  skill_id: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
                  meeting_url: "https://meet.example.com/abc-defg-hij"
        "400":
          description: |
            課程 id 不存在、或這堂課不是登入者本人開的（兩種情況回同一句訊息）。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              example:
                status: failed
                message: 課程不存在
        "401":
          description: |
            未登入或 token 有問題時觸發（這支不驗教練身分，所以沒有「使用者尚未成為教練」）。
            ⚠️ 「請先登入」是前端會拿來判斷畫面的固定錯誤訊息文字，一字不差。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無使用者
                  value:
                    status: failed
                    message: 無效的 token
    put:
      tags:
        - M3 教練後台
      summary: 更新單一課程
      description: |
        教練後台「編輯課程」modal 按儲存時呼叫。需要登入。

        行為備註：
        - ⚠️ 不支援部分更新：body 的**全部欄位都必填**，就算你只想改課程名稱，其他欄位也要原值帶上（前端表單本來就會整包送）。缺欄位或空字串回 400「欄位未填寫正確」。
        - ⚠️ `meeting_url` 必須以 `https` 開頭；`max_participants` 必須是數字型別的 0 以上整數。
        - ⚠️ owner-scoped：課程不是你開的、或 id 不存在，都回 400「課程不存在」（先驗欄位、再驗擁有者）。
        - 這支只驗登入、不驗教練身分，靠「只查得到自己開的課」做隔離。
        - 成功回 200，data.course 是更新後的完整課程物件（注意：跟 GET 詳情的扁平形狀不同，這支有包 `course` 一層）。
        - 驗收口徑：狀態碼數字供參考，驗收只看 2xx／4xx 與 body 的 `status` 欄位。
      security:
        - bearerAuth: []
      parameters:
        - name: courseId
          in: path
          required: true
          description: 課程 id（uuid）
          schema:
            type: string
          example: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - skill_id
                - name
                - description
                - start_at
                - end_at
                - max_participants
                - meeting_url
              properties:
                skill_id:
                  type: string
                  example: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
                name:
                  type: string
                  example: 重訓基礎入門（改版）
                description:
                  type: string
                  example: 課綱更新：加入自由重量的安全教學。
                start_at:
                  type: string
                  description: UTC ISO 8601 字串
                  example: "2026-08-21T10:00:00Z"
                end_at:
                  type: string
                  description: UTC ISO 8601 字串
                  example: "2026-08-21T12:00:00Z"
                max_participants:
                  type: integer
                  description: ⚠️ 必須是數字型別的 0 以上整數
                  example: 12
                meeting_url:
                  type: string
                  description: ⚠️ 必須以 https 開頭
                  example: "https://meet.example.com/abc-defg-hij"
            example:
              skill_id: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
              name: 重訓基礎入門（改版）
              description: 課綱更新：加入自由重量的安全教學。
              start_at: "2026-08-21T10:00:00Z"
              end_at: "2026-08-21T12:00:00Z"
              max_participants: 12
              meeting_url: "https://meet.example.com/abc-defg-hij"
      responses:
        "200":
          description: 更新成功，回更新後的完整課程物件（包在 data.course 裡）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      course:
                        type: object
                        properties:
                          id:
                            type: string
                            example: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
                          user_id:
                            type: string
                            example: 9a8b7c6d-5e4f-3a2b-1c0d-e1f2a3b4c5d6
                          skill_id:
                            type: string
                            example: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
                          name:
                            type: string
                            example: 重訓基礎入門（改版）
                          description:
                            type: string
                            example: 課綱更新：加入自由重量的安全教學。
                          start_at:
                            type: string
                            example: "2026-08-21T10:00:00.000Z"
                          end_at:
                            type: string
                            example: "2026-08-21T12:00:00.000Z"
                          max_participants:
                            type: integer
                            example: 12
                          meeting_url:
                            type: string
                            example: "https://meet.example.com/abc-defg-hij"
                          created_at:
                            type: string
                            example: "2026-08-01T03:00:00.000Z"
                          updated_at:
                            type: string
                            example: "2026-08-10T05:30:00.000Z"
              example:
                status: success
                data:
                  course:
                    id: 5e3b1f0a-9c1d-4e2f-8a7b-1c2d3e4f5a6b
                    user_id: 9a8b7c6d-5e4f-3a2b-1c0d-e1f2a3b4c5d6
                    skill_id: 1c8da31a-5fb2-4f2b-9d3e-6a7b8c9d0e1f
                    name: 重訓基礎入門（改版）
                    description: 課綱更新：加入自由重量的安全教學。
                    start_at: "2026-08-21T10:00:00.000Z"
                    end_at: "2026-08-21T12:00:00.000Z"
                    max_participants: 12
                    meeting_url: "https://meet.example.com/abc-defg-hij"
                    created_at: "2026-08-01T03:00:00.000Z"
                    updated_at: "2026-08-10T05:30:00.000Z"
        "400":
          description: |
            兩種觸發條件，訊息不同：①任一欄位缺漏／空字串／max_participants 非數字整數／meeting_url 不是 https 開頭 →「欄位未填寫正確」；②課程 id 不存在或不是登入者本人開的 →「課程不存在」。欄位驗證先做，擁有者檢查後做。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                欄位錯誤:
                  summary: 欄位缺漏或格式不對
                  value:
                    status: failed
                    message: 欄位未填寫正確
                課程不存在:
                  summary: id 不存在或不是本人開的課
                  value:
                    status: failed
                    message: 課程不存在
        "401":
          description: |
            未登入或 token 有問題時觸發（這支不驗教練身分）。
            ⚠️ 「請先登入」是前端會拿來判斷畫面的固定錯誤訊息文字，一字不差。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無使用者
                  value:
                    status: failed
                    message: 無效的 token
  /api/admin/coaches/revenue:
    get:
      tags:
        - M6 月營收（挑戰）
      summary: 取得教練本人指定月份的營收統計
      description: |
        教練後台「營收報表」頁呼叫。需要登入，且登入者必須已經是教練。

        ⚠️ 這支有三條「不寫出來你一定會猜錯」的隱形語意，請照著做：

        1. **算哪一個月，看「報名建立時間」**：一筆報名算進哪個月，依據是它「**被建立的時間**」，不是課程的上課時間。8 月報名 9 月的課，算 **8 月**的營收。已取消的報名不計。
        2. **年份固定是伺服器的「當年」**：`?month=` 收**英文小寫月份名**（january、february、…、december），不是數字、也不是 YYYY-MM。查 `june` 就是查「今年 6 月」，不支援跨年查詢。
        3. **營收公式（floor 放在最後一步）**：
           - 單堂均價 = **全部**購買方案的 Σprice ÷ Σcredit_amount（所有方案一起算，不是只算某一包）
           - 營收 revenue = floor(該月未取消報名筆數 × 單堂均價)——**先乘再無條件捨去**，不要先把均價捨去再乘。

        可以手算驗證的範例：系統裡共 2 個方案——「10 堂 1000 元」＋「3 堂 1000 元」→ 單堂均價 = (1000+1000) ÷ (10+3) = 2000 ÷ 13 ≈ 153.846…；該月未取消報名 2 筆 → revenue = floor(2 × 153.846…) = floor(307.69…) = **307**。

        回傳欄位語意：
        - `data.total.revenue`：上面公式算出的整數營收
        - `data.total.participants`：該月不重複的報名學員數（同一人報多堂只算 1 人）
        - `data.total.course_count`：該月未取消的**報名筆數**（⚠️ 欄位名雖然叫 course_count，實際語意是報名數，跟 revenue 公式裡乘的數字是同一個）
        - 教練還沒開過任何課 → 直接回三個 0（200 成功，不是錯誤）

        驗收口徑：狀態碼數字供參考，驗收只看 2xx／4xx 與 body 的 `status` 欄位。
      security:
        - bearerAuth: []
      parameters:
        - name: month
          in: query
          required: true
          description: ⚠️ 英文小寫月份名（january～december），不是數字、不是 YYYY-MM。年份固定為伺服器當年。
          schema:
            type: string
            enum:
              - january
              - february
              - march
              - april
              - may
              - june
              - july
              - august
              - september
              - october
              - november
              - december
          example: june
      responses:
        "200":
          description: 成功取得該月營收統計；教練沒開過任何課時回三個 0（也是成功）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      total:
                        type: object
                        properties:
                          revenue:
                            type: integer
                            description: floor(該月未取消報名筆數 × 單堂均價)，單堂均價 = 全部方案 Σprice ÷ Σcredit_amount
                            example: 307
                          participants:
                            type: integer
                            description: 該月不重複的報名學員數
                            example: 2
                          course_count:
                            type: integer
                            description: 該月未取消的報名筆數（欄位名雖叫 course_count，語意是報名數）
                            example: 2
              examples:
                有營收:
                  summary: 方案共「10堂1000元」＋「3堂1000元」、該月 2 筆未取消報名
                  value:
                    status: success
                    data:
                      total:
                        revenue: 307
                        participants: 2
                        course_count: 2
                沒開過課:
                  summary: 教練還沒開過任何課
                  value:
                    status: success
                    data:
                      total:
                        revenue: 0
                        participants: 0
                        course_count: 0
        "400":
          description: |
            month 沒帶、或不是合法的英文小寫月份名（例如送了 6、June、2026-06）時觸發。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              example:
                status: failed
                message: 欄位未填寫正確
        "401":
          description: |
            未登入、token 有問題、或登入者不是教練時觸發。
            ⚠️ 「請先登入」是前端會拿來判斷畫面的固定錯誤訊息文字，一字不差。
            觸發條件對照：沒帶 token →「請先登入」；token 過期 →「Token 已過期」；token 無效 →「無效的 token」；不是教練 →「使用者尚未成為教練」（注意是 401 不是 403）。
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                未帶token:
                  summary: 沒帶 Authorization header
                  value:
                    status: failed
                    message: 請先登入
                token過期:
                  summary: token 已過期
                  value:
                    status: failed
                    message: Token 已過期
                token無效:
                  summary: token 無效或查無使用者
                  value:
                    status: failed
                    message: 無效的 token
                不是教練:
                  summary: 登入者 role 不是 COACH
                  value:
                    status: failed
                    message: 使用者尚未成為教練
  # ===== g5-public =====
  /api/coaches:
    get:
      tags:
        - M4 公開瀏覽
      summary: 取得教練分頁列表（公開，不用登入）
      description: |
        回傳教練列表，支援分頁。`per` 是每頁筆數、`page` 是第幾頁（從 1 開始）。
        分頁要真的有作用：回第 `page` 頁、每頁最多 `per` 筆；該頁沒資料就回空陣列（仍是成功），不算錯誤。

        ⚠️ 地雷一：`per` 和 `page` 都是「必填」。少帶任何一個、或帶了不能轉成非負整數的值（例如 `per=test`），都要回失敗。message 可參考「欄位未填寫正確」，驗收重點是 4xx + status: failed。
        ⚠️ 地雷二：回傳的 `id` 是「教練 id」，不是使用者 id。前端會拿這個 `id` 去打 `GET /api/coaches/{coachId}`，`user_id` 才是這位教練對應的使用者 id，兩個欄位都要給、不要搞混。
        補充：前端頁面固定用 `per=6&page=1` 呼叫且只用到 `id`，但驗收會帶其他值測分頁與欄位完整性，不能寫死。

        狀態碼尺度：文件寫的 200／400 是實際參考值，驗收只看「2xx／4xx」加上 body 的 `status` 欄位。
      parameters:
        - name: per
          in: query
          required: true
          description: 每頁筆數，必填，必須是可轉成非負整數的字串
          example: "6"
          schema:
            type: string
        - name: page
          in: query
          required: true
          description: 第幾頁（從 1 開始），必填，必須是可轉成非負整數的字串
          example: "1"
          schema:
            type: string
      responses:
        "200":
          description: 成功取得教練列表（該頁沒資料時 data 為空陣列）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: 教練 id（uuid，拿這個去查教練詳情）
                        user_id:
                          type: string
                          description: 這位教練對應的使用者 id（uuid）
                        name:
                          type: string
                          description: 教練名字
              example:
                status: success
                data:
                  - id: "1c8da31a-5fd2-44f3-897e-4a259e7ec62b"
                    user_id: "9a1b2c3d-4e5f-6789-abcd-ef0123456789"
                    name: "肌肉小美"
        "400":
          description: 觸發條件：缺 per 或 page、或其中任何一個不是可轉成非負整數的字串
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              example:
                status: failed
                message: 欄位未填寫正確
  /api/coaches/{coachId}:
    get:
      tags:
        - M4 公開瀏覽
      summary: 取得單一教練詳細資料（公開，不用登入）
      description: |
        用教練 id（uuid 字串）查單一教練。回傳分兩塊：`user`（教練的名字與角色）和 `coach`（教練檔案，最後面掛一個 `skills` 技能名稱陣列）。

        ⚠️ 地雷一：`skills` 是「字串」陣列（例如 ["瑜伽", "重訓"]），不是物件陣列，不要包成 {id, name}。
        ⚠️ 地雷二：查不到教練時，建議回「找不到該教練」。前端會把這句話整頁顯示給使用者看；驗收重點是 4xx + status: failed。
        ⚠️ 地雷三：路徑上的 {coachId} 是列表回的「教練 id」，不是 user_id。拿 user_id 來查會查不到，回「找不到該教練」。

        補充：role 在這支查得到的教練身上固定是 COACH；profile_image_url 可能是 null（教練沒放頭像）。
        傳入非 uuid 格式的亂字串（例如 abc123）不列入驗收；建議回 400 failed，不要讓它變成 500。
        狀態碼尺度：驗收只看「2xx／4xx」加上 body 的 `status` 欄位，實際數字供參考。
      parameters:
        - name: coachId
          in: path
          required: true
          description: 教練 id（uuid 字串，從教練列表的 id 欄位取得）
          example: "1c8da31a-5fd2-44f3-897e-4a259e7ec62b"
          schema:
            type: string
      responses:
        "200":
          description: 成功取得教練詳細資料
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      user:
                        type: object
                        properties:
                          name:
                            type: string
                          role:
                            type: string
                            description: 固定是 COACH
                      coach:
                        type: object
                        properties:
                          id:
                            type: string
                            description: 教練 id（uuid）
                          user_id:
                            type: string
                            description: 對應的使用者 id（uuid）
                          experience_years:
                            type: integer
                          description:
                            type: string
                          profile_image_url:
                            type: string
                            nullable: true
                          created_at:
                            type: string
                            description: ISO 8601 時間字串
                          updated_at:
                            type: string
                            description: ISO 8601 時間字串
                          skills:
                            type: array
                            description: 技能名稱「字串」陣列
                            items:
                              type: string
              example:
                status: success
                data:
                  user:
                    name: "肌肉小美"
                    role: "COACH"
                  coach:
                    id: "1c8da31a-5fd2-44f3-897e-4a259e7ec62b"
                    user_id: "9a1b2c3d-4e5f-6789-abcd-ef0123456789"
                    experience_years: 5
                    description: "專長重訓與體態雕塑，帶你安全變壯。"
                    profile_image_url: "https://example.com/avatar.png"
                    created_at: "2026-05-01T08:00:00.000Z"
                    updated_at: "2026-05-01T08:00:00.000Z"
                    skills:
                      - "重訓"
                      - "瑜伽"
        "400":
          description: 觸發條件分兩種：(1) coachId 是合法 uuid 但查無此教練 → 「找不到該教練」；(2) coachId 為空或無效字串（例如字面的 undefined）→ 「欄位未填寫正確」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                coachNotFound:
                  summary: 查無此教練（建議回這句，前端會整頁顯示）
                  value:
                    status: failed
                    message: 找不到該教練
                invalidCoachId:
                  summary: coachId 為空或無效字串
                  value:
                    status: failed
                    message: 欄位未填寫正確
  /api/coaches/{coachId}/courses:
    get:
      tags:
        - M4 公開瀏覽
      summary: 取得指定教練「未結束」的課程列表（公開，不用登入）
      description: |
        回傳這位教練所有「未結束」的課程。判斷標準：end_at > 現在時間。
        也就是「尚未開始」和「進行中」的課都要回，只排除已經下課的。

        ⚠️ 時間口徑地雷：這支跟 GET /api/courses 的篩選條件「不一樣」，驗收會分開測——
        - 這支（教練課表）：end_at > now（未結束，含尚未開始的課）
        - GET /api/courses（全站課程）：start_at <= now < end_at（只有進行中）
        不要兩支寫成同一個條件。

        ⚠️ 欄位地雷：每筆課程要多附 coach_name（教練名字）和 skill_name（技能名稱）——這兩個不是課程自己的欄位，要自己組出來放進去。
        日期欄位 start_at／end_at 一律回 UTC ISO 8601 字串。
        教練存在但沒有未結束的課 → 回空陣列（成功），不是錯誤；查不到教練 → 失敗，message 是「找不到該教練」。
        狀態碼尺度：驗收只看「2xx／4xx」加上 body 的 `status` 欄位，實際數字供參考。
      parameters:
        - name: coachId
          in: path
          required: true
          description: 教練 id（uuid 字串，從教練列表的 id 欄位取得）
          example: "1c8da31a-5fd2-44f3-897e-4a259e7ec62b"
          schema:
            type: string
      responses:
        "200":
          description: 成功取得該教練未結束的課程（沒有課時 data 為空陣列）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: 課程 id（uuid）
                        name:
                          type: string
                        description:
                          type: string
                        start_at:
                          type: string
                          description: UTC ISO 8601 時間字串
                        end_at:
                          type: string
                          description: UTC ISO 8601 時間字串
                        max_participants:
                          type: integer
                        coach_name:
                          type: string
                          description: 教練名字（要自己組進來）
                        skill_name:
                          type: string
                          description: 技能名稱（要自己組進來）
              example:
                status: success
                data:
                  - id: "5f2c1a9e-7b3d-4e8f-9a0b-1c2d3e4f5a6b"
                    name: "晨間瑜伽"
                    description: "適合新手的伸展課程"
                    start_at: "2026-08-20T10:00:00.000Z"
                    end_at: "2026-08-20T12:00:00.000Z"
                    max_participants: 10
                    coach_name: "肌肉小美"
                    skill_name: "瑜伽"
        "400":
          description: 觸發條件分兩種：(1) coachId 是合法 uuid 但查無此教練 → 「找不到該教練」；(2) coachId 為空或無效字串 → 「欄位未填寫正確」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                coachNotFound:
                  summary: 查無此教練
                  value:
                    status: failed
                    message: 找不到該教練
                invalidCoachId:
                  summary: coachId 為空或無效字串
                  value:
                    status: failed
                    message: 欄位未填寫正確
  /api/courses:
    get:
      tags:
        - M4 公開瀏覽
      summary: 取得全站「進行中」的課程列表（公開，不用登入）
      description: |
        回傳全站「進行中」的課程。判斷標準：start_at <= 現在時間 < end_at。
        尚未開始的課和已結束的課都「不要」回。

        ⚠️ 地雷一：前端頁面「沒有」呼叫這支——做完用瀏覽器走一遍流程也不會發現它壞掉，但它在驗收範圍內（自動化驗收會直接打這支 API），不能跳過不做。
        ⚠️ 地雷二（時間口徑）：跟 GET /api/coaches/{coachId}/courses 的口徑不同——那支回「未結束」（end_at > now，含尚未開始），這支只回「已開始且未結束」。兩個條件要分開寫，驗收會分開測。

        回傳形狀跟教練課表那支一樣：每筆都要含 coach_name 與 skill_name，日期是 UTC ISO 8601 字串。
        沒有進行中的課就回空陣列（成功）。這支沒有可預期的 4xx 失敗情境。
        狀態碼尺度：驗收只看「2xx」加上 body 的 `status` 欄位，實際數字供參考。
      responses:
        "200":
          description: 成功取得進行中課程列表（沒有進行中課程時 data 為空陣列）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: 課程 id（uuid）
                        name:
                          type: string
                        description:
                          type: string
                        start_at:
                          type: string
                          description: UTC ISO 8601 時間字串
                        end_at:
                          type: string
                          description: UTC ISO 8601 時間字串
                        max_participants:
                          type: integer
                        coach_name:
                          type: string
                          description: 開課教練名字（要自己組進來）
                        skill_name:
                          type: string
                          description: 技能名稱（要自己組進來）
              example:
                status: success
                data:
                  - id: "5f2c1a9e-7b3d-4e8f-9a0b-1c2d3e4f5a6b"
                    name: "晨間瑜伽"
                    description: "適合新手的伸展課程"
                    start_at: "2026-06-10T00:00:00.000Z"
                    end_at: "2026-06-10T23:00:00.000Z"
                    max_participants: 10
                    coach_name: "肌肉小美"
                    skill_name: "瑜伽"
  # ===== g6-booking =====
  /api/courses/{courseId}:
    post:
      tags:
        - M5 購買與報名
      summary: 報名課程（學員用 token 報名一門課，最容易踩雷）
      description: |
        【重要：四句固定錯誤訊息文字都在這支 API】

        前端拿到錯誤回應後，會用 `message` 文字判斷要開哪個提示視窗。
        下面四句訊息是固定錯誤訊息文字——**多一個字、少一個字、改個標點都不行**。
        只要改了一個字，前端的報名按鈕就會看起來「沒反應」（畫面不跳任何提示），而且非常難 debug：

        1. 「已經報名過此課程」（400）
        2. 「已無可使用堂數」（400）
        3. 「已達最大參加人數，無法參加」（400）
        4. 「請先登入」（401，沒帶 token 時的回覆）

        【行為備註】
        - 這支**不需要 request body**：要報名的課由網址的 courseId 決定、報名的人由 token 決定。
        - 錯誤檢查順序（順序也要照做，先中的先回）：
          ① courseId 查無此課程 → 400「ID錯誤」
          ② 這位使用者對這門課**已有報名紀錄（包含已取消的紀錄）** → 400「已經報名過此課程」
          ③ 剩餘堂數歸零 → 400「已無可使用堂數」
          ④ 這門課目前的有效報名人數已達名額上限 → 400「已達最大參加人數，無法參加」
          全部通過 → 建立報名紀錄，回 201、data 為 null。
        - ⚠️「取消過的課不能再報名」：檢查②比對報名紀錄時**不排除已取消的**，
          所以同一門課取消後再報名，會吃到「已經報名過此課程」。這是規格，不是 bug。
        - 剩餘堂數沒有獨立欄位：剩餘堂數 ＝ 所有方案購買的堂數加總 − 未取消的報名數，
          每次報名時即時算出來。
        - 文件寫的狀態碼是實際數字供參考；驗收尺度只看「2xx／4xx ＋ 回應的 status 欄位」，
          但 message 的四句固定錯誤訊息文字沒有放寬空間。
      security:
        - bearerAuth: []
      parameters:
        - name: courseId
          in: path
          required: true
          description: 課程 id（uuid 字串）
          schema:
            type: string
            format: uuid
            example: "fb1d67ef-2b9a-4d10-8f3e-1c2d3e4f5a6b"
      responses:
        "201":
          description: 報名成功（通過全部四道檢查後建立報名紀錄）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    nullable: true
                    example: null
              example:
                status: "success"
                data: null
        "400":
          description: |
            可預期失敗，依檢查順序觸發。下列三句報名流程的固定錯誤訊息文字必須完全相同；「ID錯誤」是建議範例：
            - courseId 查無此課程 → 「ID錯誤」
            - 已有此課程的報名紀錄（含已取消） → 「已經報名過此課程」
            - 剩餘堂數歸零（購買堂數加總 − 未取消報名數 ≤ 0，沒買過方案也算） → 「已無可使用堂數」
            - 課程有效報名人數已達上限 → 「已達最大參加人數，無法參加」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                courseNotFound:
                  summary: courseId 查無此課程
                  value:
                    status: "failed"
                    message: "ID錯誤"
                alreadyBooked:
                  summary: 已有報名紀錄（包含已取消的，固定錯誤訊息文字①）
                  value:
                    status: "failed"
                    message: "已經報名過此課程"
                noCreditLeft:
                  summary: 剩餘堂數歸零（固定錯誤訊息文字②）
                  value:
                    status: "failed"
                    message: "已無可使用堂數"
                courseFull:
                  summary: 已達名額上限（固定錯誤訊息文字③）
                  value:
                    status: "failed"
                    message: "已達最大參加人數，無法參加"
        "401":
          description: |
            登入檢查失敗。⚠️「請先登入」是固定錯誤訊息文字④，前端靠這句決定要不要跳登入提示。
            - 沒帶 Authorization header、或格式不是 Bearer token → 「請先登入」
            - token 已過期 → 「Token 已過期」
            - token 無效或查無此使用者 → 「無效的 token」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                notLoggedIn:
                  summary: 沒帶 token（固定錯誤訊息文字④）
                  value:
                    status: "failed"
                    message: "請先登入"
                tokenExpired:
                  summary: token 已過期
                  value:
                    status: "failed"
                    message: "Token 已過期"
                invalidToken:
                  summary: token 無效或查無使用者
                  value:
                    status: "failed"
                    message: "無效的 token"
    delete:
      tags:
        - M5 購買與報名
      summary: 取消課程報名（軟刪除：紀錄保留、標記取消、堂數自動歸還）
      description: |
        【行為備註】
        - 取消＝**軟刪除**：報名紀錄不會被刪掉，只是在紀錄上標記「已取消」的時間。
        - 堂數自動歸還：因為剩餘堂數是「購買堂數加總 − 未取消報名數」即時算出來的，
          報名一取消，未取消數就少一，堂數自然回來——不需要另外把堂數「加回去」。
        - ⚠️ 但取消過的課**不能再報名**：報名那支（POST）的重複檢查包含已取消紀錄，
          因此同一門課取消後再報名，仍會回「已經報名過此課程」。請在報名檢查中保留這個限制。
        - 失敗條件：找不到「這位使用者、這門課、而且還沒取消」的報名紀錄 → 400「ID錯誤」。
          課程不存在、從未報名過、已經取消過——三種情況都回同一句「ID錯誤」。
        - 極罕見的情況下標記取消沒成功會回 400「取消失敗」，不在驗收範圍，知道有這回事就好。
        - 成功回 200、data 為 null。狀態碼供參考，驗收尺度只看 2xx／4xx ＋ status 欄位。
      security:
        - bearerAuth: []
      parameters:
        - name: courseId
          in: path
          required: true
          description: 課程 id（uuid 字串）
          schema:
            type: string
            format: uuid
            example: "fb1d67ef-2b9a-4d10-8f3e-1c2d3e4f5a6b"
      responses:
        "200":
          description: 取消成功（報名紀錄被標記為已取消，堂數歸還）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    nullable: true
                    example: null
              example:
                status: "success"
                data: null
        "400":
          description: |
            找不到「這位使用者對這門課、尚未取消」的報名紀錄
            （課程不存在／從未報名／已經取消過，三種情況都回這句）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                noActiveBooking:
                  summary: 沒有可取消的報名紀錄
                  value:
                    status: "failed"
                    message: "ID錯誤"
        "401":
          description: |
            登入檢查失敗：
            - 沒帶 Authorization header、或格式不是 Bearer token → 「請先登入」
            - token 已過期 → 「Token 已過期」
            - token 無效或查無此使用者 → 「無效的 token」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                notLoggedIn:
                  summary: 沒帶 token
                  value:
                    status: "failed"
                    message: "請先登入"
                tokenExpired:
                  summary: token 已過期
                  value:
                    status: "failed"
                    message: "Token 已過期"
                invalidToken:
                  summary: token 無效或查無使用者
                  value:
                    status: "failed"
                    message: "無效的 token"
  /api/upload:
    post:
      tags:
        - 加分題
      summary: 上傳圖片（選做加分題，不列驗收）
      description: |
        【選做加分題，不列入驗收】
        - 這支**不在驗收範圍**，前端成品也完全沒有呼叫它（教練頭像是手動貼圖片網址）。
        - 答案版的行為只是「示意」：不管你傳什麼（甚至不傳檔案），都回一個**固定寫死的 image_url**，
          沒有真的儲存任何檔案。本文件照答案版行為撰寫。
        - 想拿加分：把它做成真的圖片上傳——收 multipart/form-data 的圖片檔、
          存到任一個你選的雲端圖片空間、回傳一個真的可以打開的圖片網址。
          建議限制：只收 jpg／png、單檔 2MB 以內。
        - 一樣需要登入：沒帶 token 回 401「請先登入」。
      security:
        - bearerAuth: []
      requestBody:
        required: false
        description: |
          答案版示意行為不會讀取檔案內容，傳不傳都回固定網址。
          若要真做加分版，請用 multipart/form-data、欄位名稱用 file。
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: 圖片檔（jpg／png，建議 2MB 以內），欄位名稱用 file，例如 avatar.png
      responses:
        "200":
          description: 上傳成功（答案版回固定示意網址；加分版回真實圖片網址）
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: success
                  data:
                    type: object
                    properties:
                      image_url:
                        type: string
                        example: "https://example.com/your-uploaded-image.jpg"
              example:
                status: "success"
                data:
                  image_url: "https://example.com/your-uploaded-image.jpg"
        "401":
          description: |
            登入檢查失敗：
            - 沒帶 Authorization header、或格式不是 Bearer token → 「請先登入」
            - token 已過期 → 「Token 已過期」
            - token 無效或查無此使用者 → 「無效的 token」
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: failed
                  message:
                    type: string
              examples:
                notLoggedIn:
                  summary: 沒帶 token
                  value:
                    status: "failed"
                    message: "請先登入"
                tokenExpired:
                  summary: token 已過期
                  value:
                    status: "failed"
                    message: "Token 已過期"
                invalidToken:
                  summary: token 無效或查無使用者
                  value:
                    status: "failed"
                    message: "無效的 token"
